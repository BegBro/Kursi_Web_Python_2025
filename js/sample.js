// Скрипт sample.js //Знак комментирования js

/*
Многострочный
комментарий
подробнее здесь: https://learn.javascript.ru/
*/

// var - объявление переменной (глобальная(видна отовсюду)) let - видна до конца блока

// Функция
//function sayHello(name) {
//    document.writeln("Вас зовут " + name);
//}

// Переменная (var или let)
/*let name = prompt("Ваше имя: ");
sayHello(name);*/
// Массивы
//let colors = ["Красный","Синий","Голубой"]

/*
document.writeln("<h1>Цвета</h1><ol>");
// Цикл for
for (let i=0; i<colors.length; i++) {
    document.writeln("<li>" + colors[i] + "</li>");
}
document.write("</ol>");
*/

/*
function changeColor() {
//    document.getElementById('alive').style.color = 'red';
    const txt = document.getElementById('alive');
    if(txt.style.display === 'none') {
    txt.style.display = block;
    } else {
        txt.style.display = 'none';
    }
}
*/
// Подключаюсь к элементу в DOM (Document Object Model)

//const txt = document.getElementById('alive').onclick = changeColor;

